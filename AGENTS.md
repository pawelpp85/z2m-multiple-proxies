# Agent instructions

- When Zigbee2MQTT internals are needed, you may optionally consult a local checkout at `external/zigbee2mqtt`.
- Ask before cloning or updating the Zigbee2MQTT repository so the user can confirm.
- Keep that checkout out of git by relying on `.gitignore`.
